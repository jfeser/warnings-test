let f x = 0

type ext = ..

module Test : sig type s val yyy : s end = struct
  let xxx = 0
    type t = int
type s = A | B | C
           let yyy = B
type ext += Test

module Unused = struct
  end
end
    
let g = 1

module Test1 = struct
  open Test
let yyy = 0
end

let unused_for =
  for i = 0 to 10 do
    ()
  done

class parent = object
end

class child = object
  inherit parent as super
end

let rec non_rec x = x + 1

